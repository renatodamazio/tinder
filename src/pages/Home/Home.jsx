import React from "react";
import Button from "../UI/Buttons/Buttons";
export const Home = () => {
  return (
    <div>
      <Button variant="primary-button">
        Create Account
      </Button>
    </div>
  );
};

export default Home;
